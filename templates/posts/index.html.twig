<!DOCTYPE html>
<html>
<head>
    <title>Lista postów</title>
</head>
<body>
<h1>Lista postów</h1>
<table>
    <thead>
    <tr>
        <th>ID</th>
        <th>Tytuł</th>
    </tr>
    </thead>
    <tbody>
    {% for post in posts %}
        <tr>
            <td>{{ post.id }}</td>
            <td>{{ post.title }}</td>
            <td>{{ post.body }}</td>

        </tr>
    {% endfor %}
    </tbody>
</table>
</body>
</html>